spring:
  datasource:
    url: jdbc:postgresql://crud-example-pg/paste
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
logging:
  file: docker/logs/log.file